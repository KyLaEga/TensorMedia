name: Build and Release TensorMedia
on:
  push:
    tags:
      - 'v*' # Запуск при создании тега версии, например v1.0.0

jobs:
  build:
    name: Build ${{ matrix.artifact_name }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: macos-latest
            artifact_name: macOS
          - os: windows-latest
            artifact_name: Windows
          - os: ubuntu-22.04 # Используем 22.04 для лучшей совместимости GLIBC
            artifact_name: Linux-CPU
            type: cpu

    steps:
    - uses: actions/checkout@v4
    
    - uses: actions/setup-python@v5
      with:
        python-version: '3.12.13'
        cache: 'pip' # Кэширование зависимостей pip
    
    - name: Install System Dependencies (Linux)
      if: startsWith(matrix.os, 'ubuntu')
      run: |
        sudo apt-get update
        sudo apt-get install -y libgl1 libegl1 libxkbcommon-x11-0 libxcb-icccm4 libxcb-image0 libxcb-keysyms1 libxcb-randr0 libxcb-render-util0 libxcb-xinerama0 libxcb-xfixes0 xdg-utils

    - name: Install Base Python Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pyinstaller

    - name: Install PyTorch (Linux CPU Version)
      if: matrix.type == 'cpu'
      run: pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu

    - name: Install Project Dependencies
      run: pip install -r requirements.txt

    - name: Cache ML Models
      id: cache-models
      uses: actions/cache@v4
      with:
        path: models
        key: ml-models-${{ hashFiles('setup_models.py') }}

    - name: Fetch ML Models into CI
      if: steps.cache-models.outputs.cache-hit != 'true'
      env:
        PYTHONIOENCODING: "utf8"
      run: python setup_models.py

    - name: Compile Binary (Cross-Platform)
      run: pyinstaller --noconfirm TensorMedia.spec

    - name: List Dist Directory (Debug)
      run: |
        ls -R dist/
      shell: bash

    - name: Build Windows Installer (.exe)
      if: matrix.os == 'windows-latest'
      run: |
        # Inno Setup (iscc) предустановлен на windows-latest
        iscc setup.iss

    - name: Build macOS Image (.dmg)
      if: matrix.os == 'macos-latest'
      run: |
        mkdir -p dist/dmg_root
        cp -R dist/TensorMedia.app dist/dmg_root/
        ln -s /Applications dist/dmg_root/Applications
        hdiutil create -volname "TensorMedia" -srcfolder dist/dmg_root -ov -format UDZO dist/TensorMedia-macOS.dmg

    - name: Build Linux Archive (.tar.gz)
      if: startsWith(matrix.os, 'ubuntu')
      run: |
        cd dist
        tar -czvf TensorMedia-Linux-x86_64.tar.gz TensorMedia/

    - name: Upload to Release
      uses: softprops/action-gh-release@v2
      with:
        files: |
          dist/TensorMedia_Setup.exe
          dist/TensorMedia-macOS.dmg
          dist/TensorMedia-Linux-x86_64.tar.gz
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
