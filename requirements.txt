# ============================================================
# TensorMedia Dependencies
# ============================================================
torch>=2.2.0
# Pinned <5: transformers 5.x's is_torch_available() HARD-requires torch>=2.4 and
# silently DISABLES a torch below that → "SiglipVisionModel requires the PyTorch
# library but it was not found" at scan time (even though torch is bundled and
# FaceNet works). Our torch resolves to 2.2.2 (numpy<2 → old torchvision → old
# torch), so transformers must stay on the 4.x line — it supports SigLIP and has
# no torch>=2.4 gate. Lift this cap only together with the numpy<2 / torch bump.
transformers>=4.38.0,<5
faiss-cpu>=1.8.0
# Pinned <2: PySide6 6.6.3.1 / shiboken6 (and the prebuilt faiss/torch wheels)
# are compiled against the NumPy 1.x C-ABI. Allowing numpy 2.x makes the C
# extensions emit "compiled using NumPy 1.x cannot be run in NumPy 2.x ... may
# crash" at import. Keep both bounds so pip resolves a single ABI plane.
numpy>=1.26.4,<2
Pillow>=10.2.0
# Capped <4.10: opencv-python 4.10+ wheels require numpy>=2, which clashes with
# the numpy<2 pin above (PySide6/shiboken ABI). 4.9.0.80 is the last numpy-1.x
# build. Lift both this cap and the numpy cap together if PySide6 is upgraded
# to a numpy-2 build.
opencv-python-headless>=4.9.0,<4.10
PySide6==6.6.3.1
huggingface-hub>=0.20.3
facenet-pytorch>=2.5.3
blake3>=1.0.0
send2trash>=1.8.2
psutil>=5.9.0
sentencepiece>=0.2.0
pypdfium2>=4.30.0
protobuf>=4.25.0
watchdog>=4.0.0
# macOS specific for native trash support
pyobjc-framework-Cocoa>=12.0; sys_platform == 'darwin'